select * from dept
select * from emp
select * from students

-- Create the Employee table
CREATE TABLE Employee (
    EmpNo INT PRIMARY KEY,
    FName VARCHAR(50),
    Salary DECIMAL(10, 2),
    DeptNo INT
);

-- Insert 10 records into the Employee table
INSERT INTO Employee (EmpNo, FName, Salary, DeptNo) VALUES
(101, 'Alice', 55000.00, 10),
(102, 'Bob', 62000.00, 20),
(103, 'Charlie', 48000.00, 10),
(104, 'David', 70000.00, 30),
(105, 'Eve', 53000.00, 20),
(106, 'Frank', 60000.00, 10),
(107, 'Grace', 75000.00, 30),
(108, 'Hank', 58000.00, 20),
(109, 'Ivy', 67000.00, 10),
(110, 'Jack', 72000.00, 30);
select E.* from employee e where Salary=(SELECT MAX (Salary) FROM Employee EM WHERE EM.deptno=E.DeptNo)
--max no of employee in which dept--
select DeptNo from Employee e
group by deptno
having count(*)>=all (select count(*) from employee em 
group by DeptNo)
--name of the person geetbing the second highest sal--
select FName from Employee where Salary=
(select max(Salary) from Employee where Salary <
(select max(salary) from Employee  ))
alter table employee
ALTER TABLE Employee
ADD Manager int
update employee
set manager = null where  empno =101
select * from Employee
select e1.fname,e2.fname as manager from Employee e1,Employee e2
where e1.manager=e2.empno
